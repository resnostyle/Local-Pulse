# OpenAI (for event normalization of html sources)
OPENAI_API_KEY=

# JSON file pipeline — local data directory (default: ./data)
# LOCALPULSE_DATA_ROOT=./data

# Cloudflare R2 (GitLab CI ingest job — set in GitLab CI/CD variables or Vault)
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=
# R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
